# OCaml debug-info overlay

This overlay exposes OCaml compilers with debug symbols and source code to 
ease runtime debugging.

To see how to integrate this overlay using `flake-parts`, check out the starter 
template or run:
```console
nix flake init -t github:Halbaroth/ocaml-debug-info-overlay
```
